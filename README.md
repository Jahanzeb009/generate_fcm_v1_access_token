# This repo is used to generate the FCM v1 access token

### Add your **google_service_account.json** in the root directory and run the project using the below command;

```
node app
```
That's all.

### Thanks!
